请做以下修改，分两大块：A是之前讨论的4个bug修复，B是新增的质量提升。

## A. Bug修复（4项）

**A1. Part2重复Part1内容**

检查 `stage_write_story` 中写第2部分时传入的前文内容是否正确。在 `prompts/03_write_part.md` 的绝对禁止列表里加上：
```
- **绝对禁止重复前文已有的场景和对话。** 如果前文末尾已经写到了某个打脸场景的结束，你必须从该场景之后开始写全新的剧情推进。不要换个说法把同一件事再写一遍。
```

**A2. 逐句编号问题**

在 `prompts/03_write_part.md` 里加强编号规则：
```
- 数字编号（1. 2. 3.）只用于分隔大的场景段落，每个编号段至少包含10-20句话、500字以上。一篇10000字的故事，编号通常只有8-15个。如果你的编号段只有1-3句话，说明你用错了——去掉编号，让这些句子作为自然段落排列。
```

**A3. 打脸方式重复**

在 `stage_write_story` 中，写完每一部分后，提取该部分用了什么打脸方式（可以简单用一句话概括），存到一个列表里，写下一部分时在 prompt 里注入：
```
## 前文已使用过的打脸方式（禁止重复）
{used_face_slaps}
```
在 `prompts/03_write_part.md` 里加上对应的占位符和规则：
```
- 前文已经用过的打脸方式绝对不能再用。每次打脸必须换一种全新的方式。可选：四两拨千斤、借力打力、群众打脸、装傻反杀、以彼之道还施彼身。
```

**A4. 字数不足+截断**

`config.yaml` 里把 `write_parts` 从3改成4，`words_per_part` 从3000改成2500。同步修改 `stage_write_story` 里的分段逻辑，从3段改成4段：
- 第1部分：开头→第一次打脸后
- 第2部分：第一次打脸后→中段发展
- 第3部分：中段→低谷/转折
- 第4部分：转折→结局

每段的大纲范围描述也要相应调整。第1部分仍然走 `eval_and_fix_loop`，第2-4部分走 `quick_check`。

## B. 新增质量提升（3项）

**B1. 角色设定合理性检查**

新建 `prompts/02b_character_check.md`：
```markdown
# 角色设定合理性检查

你是一位经验丰富的小说编辑，检查以下角色设定是否存在问题。

## 角色列表
{characters_json}

## 检查项（JSON输出）

```json
{
  "surname_conflict": {
    "found": true/false,
    "detail": "是否有不相关的角色撞姓？亲子关系共姓合理，其他角色应避免同姓（除非剧情需要）"
  },
  "personality_depth": [
    {
      "name": "角色名",
      "has_dual_side": true/false,
      "suggestion": "如果性格太单一（纯好人/纯坏人），建议增加什么矛盾面？例如：反派虽然狠毒但对某个人有软肋；好人虽然善良但有致命弱点"
    }
  ],
  "relationship_clarity": {
    "ok": true/false,
    "detail": "角色之间的关系是否清晰？有没有角色的存在感不明确？"
  },
  "needs_fix": true/false,
  "fix_suggestions": ["具体修改建议1", "具体修改建议2"]
}
```

在 `pipeline.py` 的 `stage_setting_outline` 中，大纲生成后、试读者评估前，插入一次角色检查。如果检查发现问题（`needs_fix=true`），把 `fix_suggestions` 作为额外指令传入重新生成大纲。


**B2. 大纲场景转换检查**

在 `prompts/02_setting_outline.md` 的关键规则里加上：
```
8. **每个场景段必须有隐含的时间、地点、在场人物。** 不需要写"时间：XX 地点：XX"，但读者必须能从对话内容、动作描写或环境细节中自然推断出场景。例如"她端着咖啡走进来"就暗示了办公室/家里，"窗外的蝉鸣吵得人心烦"就暗示了夏天。大纲的 events 描述中，每个场景段标注一下主要地点。
9. **场景切换不能跳跃太大。** 如果上一段在家里，下一段突然在公司，中间至少需要一句过渡（哪怕是"第二天一早"这样的时间词）。大纲里标注场景切换点。
```

**B3. 允许龙套角色**

在 `prompts/02_setting_outline.md` 里，把角色规则从"不超过4个"改成：
```
2. **有名有姓的核心角色不超过4个。** 但可以有不限数量的"龙套"——没有名字的功能性角色，如管家、丫鬟、路人、同事、围观群众等。龙套的作用是：点缀场景真实感、推动剧情（传话、通风报信）、制造群体反应（窃窃私语、鼓掌、起哄）。龙套不需要写进角色表，但大纲的 events 里可以写"围观同事议论纷纷"或"管家来报说客人到了"。
```

同步在 `prompts/03_write_part.md` 里加一条：
```
- **善用龙套角色丰富场景。** 可以让没名字的配角说一两句话或做一个动作来推动剧情。例如"旁边有人低声说：'她不会真敢吧？'""保姆端着托盘进来，看了一眼气氛，又悄悄退了出去"。龙套让场景有生活感，不要让每个场景都只有核心角色在对话。
```