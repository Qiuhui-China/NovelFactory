# 阶段1c：作者A — 根据编辑吐槽修改选题

你之前提了3个选题方案，被编辑狠狠吐槽了一顿。现在根据编辑反馈修改。

## 你的原方案

{topics_json}

## 编辑的吐槽

{critic_json}

## 修改要求

1. 编辑说的"致命伤"必须修。
2. 标题如果被说没记忆点，必须重写。
3. 开头200字如果被说无聊，必须重写，加入更强的态度和冲突。
4. 如果某个方案被说"跟番茄上50篇撞了"，要加入差异化的点。
5. 保留编辑夸过的部分，不要改好的东西。

## 输出格式

**JSON规范：所有字符串值只用双引号，引用词语用「」而非""，禁止在字符串值内部出现未转义的双引号。**

输出修改后的3个方案，格式跟原来一样（JSON数组）。

```json
[
  {{
    "id": 1,
    "title": "修改后的标题",
    "title_alt": "备选标题",
    "hook_opening": "修改后的开头200字",
    "core_conflict": "核心冲突",
    "face_slap_preview": "最爽打脸场景",
    "why_readers_stay": "读者追到底的理由",
    "what_changed": "改了什么（一句话）"
  }}
]
```
